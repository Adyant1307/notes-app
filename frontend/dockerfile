# AI-assisted scaffold: review & refactor before commit
FROM node:20-alpine

WORKDIR /app

# Install deps with clean, reproducible lockfile
COPY package*.json ./
RUN npm ci

# Copy app code
COPY . .

# Expose & default cmd (dev server configured in compose)
EXPOSE 3000
CMD ["npm","run","dev"]